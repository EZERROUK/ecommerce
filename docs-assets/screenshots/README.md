# Captures d’écran

Les captures d’écran sont générées automatiquement via le script :

- `scripts/generate-screenshots.ps1`

Conventions de nommage :

- `Fxx-*` : Frontend (SPA dans `frontend/`)
- `Bxx-*` : Backoffice / Portail (UI servie par `Backend/` via Inertia)
