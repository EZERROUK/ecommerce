# Base URL consommée par le frontend (recommandé: laisser "/api" + utiliser le proxy Vite)
VITE_API_BASE_URL=/api

# Cible du proxy Vite pour /api en dev (Laravel)
# - si tu lances Laravel via `php artisan serve`: http://localhost:8000
# - si tu utilises un vhost Laragon (ex: http://xpanel.test): mets l'URL ici
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
